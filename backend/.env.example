PORT=8080
MQTT_URL=mqtt://localhost:1883
MQTT_USERNAME=
MQTT_PASSWORD=
CORS_ORIGIN=http://localhost:5173
SIMULATE_MCU_ACK=true
DATABASE_URL=postgres://armieum:armieum_dev_pw@localhost:5432/armieum
JWT_SECRET=change-me-in-real-deployment

# Only used by `npm run start:render` on a hosted deployment. Seeds one demo
# hub and runs its simulated process in the same container, so visitors can
# try claim -> Wi-Fi provisioning -> dashboard without real hardware.
DEMO_HUB_ID=
DEMO_HUB_SECRET=
DEMO_PORTAL_PORT=4001
